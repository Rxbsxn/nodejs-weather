// module.exports = {
//   capitalizeFirstLetter: function(str) {
//     return str.charAt(0).toUpperCase() + str.slice(1);
//   }
// }

module.exports = (str) => {
  return str.charAt(0).toUpperCase() + str.slice(1);
}
